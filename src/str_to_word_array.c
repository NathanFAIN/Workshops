/*
** EPITECH PROJECT, 2019
** str_to_word_array.c
** File description:
** functions for str_to_word_array
*/

#include "str_to_word_array.h"

/***************************************************
La fonction count_c_in_str comptera le nombre de
caractères c dans la chaine de caractères str.
****************************************************/
int count_c_in_str(char *str, char c)
{
    (void) str; //Pour éviter les warning, à retier
    (void) c;   //lors ce que tu commences à coder.

    return (0);
}



/***************************************************
La fonction count_before_c_in_str comptera le nombre
de caractères avant de rencontrer le caractère c ou
'\0' dans la chaine de caractères str.
****************************************************/
int count_before_c_in_str(char *str, char c)
{
    (void) str; //Pour éviter les warning, à retier
    (void) c;   //lors ce que tu commences à coder.

    return (0);
}



/***************************************************
La fonction my_strndup dupliquera la chaine de
caractères str en mettant dedans n caractères.
Tu connais les fonctions my_strcpy et my_strncpy,
les fonctions my_strdup et my_strndup marchent à
peu près pareil...
Je t'invite à voir ce site pour plus d'info:
http://manpagesfr.free.fr/man/man3/strdup.3.html
****************************************************/
char *my_strndup(char *str, int size)
{
    (void) str; //Pour éviter les warning, à retier
    (void) size;   //lors ce que tu commences à coder.

    return (NULL);
}



/***************************************************
La fonction str_to_word_array "transforme" ta chaine
de caractères en un tableau de chaines caractères à
l'aide d'un séparateur, exemple:
Entrée -> "42\nest\nla\nréponse!"
Sortie -> "42", "est", "la", "réponse!"

Tu es fin près pour coder la fameuse fonction
str_to_word_array, utilises les fonctions que tu as
codé juste au dessus pour t'aider.
****************************************************/
char **str_to_word_array(char *str, char c)
{
    (void) str; //Pour éviter les warning, à retier
    (void) c;   //lors ce que tu commences à coder.

    return (NULL);
}
